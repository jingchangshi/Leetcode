#include <iostream>
#include <vector>
#include <string>

using namespace std;

class Solution {
  public:
    bool isMatch(string s, string p) {
      
    }
};

int main(void) {
  string s;
  string p;
  s = "aa";
  p = "a";
  Solution sol;
  bool res = sol.isMatch(s, p);
  cout << res << endl;
  return 0;
}
